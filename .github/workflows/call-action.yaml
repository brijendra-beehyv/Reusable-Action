name: Use custom action

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Call action
        uses: brijendra-beehyv/Reusable-Action/.github/actions/hello@main
        with:
          name: Brijendra Singh

