name: Use custom action

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: checkout local repo action
        uses: actions/checkout@v4

      - name: Call action
        uses: ./.github/actions/hello
        with:
          name: Brijendra Singh

